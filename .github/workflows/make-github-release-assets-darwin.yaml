name: make-github-release-assets-darwin.yaml

on: [push]

# on:
#   push:
#     tags:
#       - "[0-9]+.[0-9]+.[0-9]+"

jobs:
  build:
    name: Publish binaries - darwin
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Install create-dmg
        run: brew install create-dmg
      - name: Download SenzingApi
        run: curl --output /tmp/senzingapi.dmg https://public-read-access.s3.amazonaws.com/staging/senzingapi_3.7.0.23207.dmg
      - name: Attach .dmg
        run: hdiutil attach /tmp/senzingapi_3.7.0.23207.dmg
      - name: Copy files
        run: mkdir -p /opt/senzing/ && sudo cp /Volumes/SenzingAPI/senzing/g2 /opt/senzing
      - name: Make executable file
        run: make darwin/amd64
      - name: Adjust create-dmg
        run: |
          sed -e "s|ln -s /Applications|ln -s /usr/local/bin|" /usr/local/bin/create-dmg > /usr/local/bin/create-dmg-new
          mv /usr/local/bin/create-dmg /usr/local/bin/create-dmg-backup
          mv /usr/local/bin/create-dmg-new /usr/local/bin/create-dmg
          chmod +x /usr/local/bin/create-dmg
      - name: Make a .dmg file
        run: |
          export BUILD_VERSION=$(git describe --always --tags --abbrev=0 --dirty  | sed 's/v//')
          create-dmg --app-drop-link 0 40 "explain-$BUILD_VERSION.dmg"  "target/darwin-amd64/"
      - name: Upload versioned package to release assets
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: "/Users/runner/work/explain/explain/explain-*"
          file_glob: true
          tag: ${{ github.ref }}
          overwrite: true
